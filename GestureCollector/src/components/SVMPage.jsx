import React from "react";
import GestureCollector from "../GestureCollector";

const SVMPage = () => {
  return (
    <div>
      <h2>SVM Classifier</h2>
      <GestureCollector model="svm" />
    </div>
  );
};

export default SVMPage;
